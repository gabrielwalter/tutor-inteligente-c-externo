# Google Gemini API Key (Required)
# Get your key from: https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key_here

# PostgreSQL Connection (Optional - for authentication and progress features)
# Format: postgresql://username:password@host:port/database
# Examples:
#   - Supabase: postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres
#   - ElephantSQL: postgresql://[user]:[pass]@[host].db.elephantsql.com/[user]
#   - Local: postgresql://postgres:postgres@localhost:5432/tutor_db
DATABASE_URL=postgresql://username:password@localhost:5432/database

# JWT Secret (Optional - for authentication)
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_here

# Node Environment (Optional)
# Set to 'production' for deployment
NODE_ENV=development

# Server Port (Optional)
# Default: 3000
PORT=3000

# Server Host (Optional)
# Default: 0.0.0.0
HOST=0.0.0.0
